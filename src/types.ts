export enum ScaleMode {
  None,
  Fit,
  FitWidth,
  FitHeight,
  Fill,
  Stretch,
}

export enum Center {
  None,
  Horizontal,
  Vertical,
  Both,
}

export enum ClearFlag {
  None = 0,
  Depth,
  Color,
}
